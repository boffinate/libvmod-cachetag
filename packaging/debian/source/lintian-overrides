# There is no upstream watch URL to track. The source package is built from the
# tagged release archive named in the cohort manifest, whose digest is pinned
# and verified by the release tooling; a watch file would describe a weaker
# check than the one already performed.
libvmod-cachetag source: debian-watch-file-is-missing
